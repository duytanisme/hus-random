[project]
name = "demo-python"
version = "0.1.0"
description = "Social Network Propagation Analysis - Random Walk vs Content-biased Random Walk"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "networkx>=3.2.1",
    "matplotlib>=3.8.2",
    "numpy>=1.26.2",
]
